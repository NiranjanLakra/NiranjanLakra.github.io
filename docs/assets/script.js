// JavaScript can be used to add interactive features or dynamically load content if necessary.
// For a resume, it can be used to add print functionality or download the resume as a PDF.

document.addEventListener('DOMContentLoaded', () => {
  console.log('Resume loaded');
  // Add more interactive functionality if needed
});
